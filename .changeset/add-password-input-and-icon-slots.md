---
"@quickadui/forms": minor
---

Add `PasswordInput` (with an optional `PasswordStrengthMeter`) and `startIcon`/`endIcon` slot props on `Input` for rendering icons at the start or end of a text field.
