// @quickadui/config/tailwind-preset
//
// Placeholder. The real preset is generated from @quickadui/tokens at build
// time (Blueprint §7) — it will export the Tailwind v4 `@theme` values so a
// consumer app's own Tailwind config can extend QuickadUI's token scale
// instead of redefining it.
export default {};
