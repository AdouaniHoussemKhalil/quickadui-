# QuickadUI Docs (scaffold)

Placeholder for the dogfooded Vite + MDX documentation site described in
the QuickadUI Blueprint, §10: generated prop tables, live Sandpack
playgrounds, versioned per published major.

Not built yet — this directory exists so the workspace glob and CI
pipeline are already correct when it is.
