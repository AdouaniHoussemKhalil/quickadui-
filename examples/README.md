# Examples

One minimal, runnable example per package. Imported by both the docs site
and CI, so a broken example fails the build instead of a visitor's
browser. Populated alongside each package's first real implementation.
