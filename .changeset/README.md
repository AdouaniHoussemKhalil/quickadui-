# Changesets

Every PR that changes a published package's behavior needs a changeset:
run `pnpm changeset`, describe the change in one or two sentences, pick
the affected packages and bump type. See the QuickadUI Blueprint, §13.
